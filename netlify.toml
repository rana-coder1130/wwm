[build]
  command = "npm run build"
  publish = ".svelte-kit/output/publish"

# If you later enable split mode in adapter-netlify (functions),
# add the functions directory here, for example:
# [functions]
#   directory = ".svelte-kit/output/functions"
